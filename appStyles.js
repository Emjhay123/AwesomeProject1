import { StyleSheet } from "react-native";

const styles = StyleSheet.create ({ //Create a style sheet declaration
    container:{
        alignItems: 'center',
    },
    title:{
        fontSize: 28,
        color: '#002147',
        fontWeight: '900',
        marginTop: 10,
        marginBottom: 10,
        fontFamily: 'sans-serif',
    }
});

export default styles; //export the variable where the stylesheet data is stored